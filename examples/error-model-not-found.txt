Command:
pisight --config config.yaml recognize --no-window

Example stderr:
error: No enrolled face vectors found. Run the collect command first.

Checks:
- Run pisight --config config.yaml collect --name demo-user-001 --count 10.
- Confirm paths.vector_index_path and paths.vector_labels_path match the generated files.
